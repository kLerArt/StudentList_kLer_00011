package programable.android.klerart.showuseronrecyclerview;import android.Manifest;import android.content.Intent;import android.content.pm.PackageManager;import android.graphics.Bitmap;import android.graphics.BitmapFactory;import android.graphics.drawable.BitmapDrawable;import android.net.Uri;import android.os.Bundle;import android.view.View;import android.widget.Button;import android.widget.EditText;import android.widget.ImageView;import android.widget.RadioButton;import android.widget.RadioGroup;import android.widget.Spinner;import android.widget.Toast;import androidx.annotation.NonNull;import androidx.appcompat.app.AppCompatActivity;import androidx.core.app.ActivityCompat;import java.io.ByteArrayOutputStream;import java.io.FileNotFoundException;import java.io.InputStream;import programable.android.klerart.showuseronrecyclerview.Database.Database;public class ChangeStudentInfo extends AppCompatActivity {    ImageView MyImga;    final  int REQUEST_CODE_GALLERY=999;    EditText StudenName,StuId;    Button ChangeName,ChangeID,AllIn;    Spinner SSimester,SGroup,SDepartment,SStudentYear;    RadioGroup Seson;    RadioButton SeasoonSelected;    int Smester,mGroup,Department,StuSeason,SYStudentYear=0;    @Override    protected void onCreate(Bundle savedInstanceState) {        super.onCreate(savedInstanceState);        setContentView(R.layout.activity_change_student_info);        Inta();        AllIn=findViewById(R.id.AllIn);        AllIn.setOnClickListener(new View.OnClickListener() {            Database database=new Database(getApplicationContext());            @Override            public void onClick(View view) {                if (SeasoonSelected.getText()=="خريفي"){                    StuSeason=1;                }else  if (SeasoonSelected.getText()=="ربيعي"){                    StuSeason=2;                }else StuSeason=0;//                SYStudentYear=SStudentYear.getSelectedItem().toString().trim();                if (SStudentYear.getSelectedItem().toString()=="2020"){                    SYStudentYear=2020;                }else  if (SeasoonSelected.getText()=="2021"){                    SYStudentYear=2021;                }else                if (SStudentYear.getSelectedItem().toString()=="2022"){                    SYStudentYear=2022;                }else  if (SeasoonSelected.getText()=="2023"){                    SYStudentYear=2023;                }else if (SStudentYear.getSelectedItem().toString()=="2018"){                    SYStudentYear=2018;                }else  if (SeasoonSelected.getText()=="2019"){                    SYStudentYear=2019;  }else SYStudentYear=2020;                Intent intent=getIntent();                intent.getExtras();                int OId=intent.getIntExtra("OrginalID",0);                String M=String.valueOf(OId);                try {//                   int Smester,mGroup,Department,StuSeason=0;getSelectedItemPosition()      SSimester,SGroup,SDepartment,SStudentYear;                    database.UpDataStudentDataInfo(M,StudenName.getText().toString().trim(),StuId.getText().toString(),                            StuSeason,SYStudentYear,SDepartment.getSelectedItemPosition()-1,SSimester.getSelectedItemPosition(),SGroup.getSelectedItemPosition(),imageViewToByte(MyImga));                    Toast.makeText(ChangeStudentInfo.this,"U got This"+  SSimester.getSelectedItemPosition(),Toast.LENGTH_LONG).show();//                    database.insertNote(edtName.getText().toString().trim(),editID.getText().toString(),imageViewToByte(imageView));                }catch (Exception e){                    e.getMessage();                    Toast.makeText(ChangeStudentInfo.this,"error is "+e.getMessage(),Toast.LENGTH_LONG).show();                }            }        });        MyImga.setOnClickListener(new View.OnClickListener() {            @Override            public void onClick(View view) {                ActivityCompat.requestPermissions(                        ChangeStudentInfo.this,                        new String[]{Manifest.permission.READ_EXTERNAL_STORAGE},                        REQUEST_CODE_GALLERY                );            }        });        ChangeName.setOnClickListener(new View.OnClickListener() {            String Name;            Database database=new Database(getApplicationContext());            @Override            public void onClick(View view) {                Intent intent=getIntent();                intent.getExtras();                Name=StudenName.getText().toString();                int OId=intent.getIntExtra("OrginalID",0);                String M=String.valueOf(OId);                database.UpdatNeme(M,Name);                Toast.makeText(ChangeStudentInfo.this,M+"   Wallla  "+Name,Toast.LENGTH_LONG).show();            }        });        ChangeID.setOnClickListener(new View.OnClickListener() {            String ID;            Database database=new Database(getApplicationContext());            @Override            public void onClick(View view) {                Intent intent=getIntent();                intent.getExtras();                ID=StuId.getText().toString();                int OId=intent.getIntExtra("OrginalID",0);                String M=String.valueOf(OId);                database.UpdatID(M,ID);                Toast.makeText(ChangeStudentInfo.this,M+"   Wallla  "+ID,Toast.LENGTH_LONG).show();            }        });//        MyImga.setImageBitmap();        Intent intent=getIntent();        intent.getExtras();        String NName=intent.getStringExtra("name");        StudenName.setText(""+NName);//        Bitmap MyBitmap = intent.getParcelableExtra("BitmapImage");//        MyImga.setImageBitmap(MyBitmap);        int IID=intent.getIntExtra("StID",0);        StuId.setText(""+IID);//        MyImga.setImageBitmap(BitmapFactory.decodeByteArray(intent.getByteArrayExtra("myImga"),0,intent.getByteArrayExtra("myImga").length));    }   /* public Void UpDataName(){        Intent intent=getIntent();        intent.getExtras();        Name=intent.getStringExtra("name");        OID=intent.getStringExtra("OrginalID");        database.UpdatNeme(OID,Name);       return null;    }*/    public void Inta(){        SSimester=findViewById(R.id.editSemester);        SDepartment=findViewById(R.id.editDepartment);        SGroup=findViewById(R.id.editGroup);        SStudentYear=findViewById(R.id.StudentYear);        Seson=findViewById(R.id.radioSeason);        int selectedId = Seson.getCheckedRadioButtonId();        SeasoonSelected= findViewById(selectedId);        StudenName=findViewById(R.id.edtName);        ChangeName=findViewById(R.id.ChangeName);        ChangeID=findViewById(R.id.ChangeID);        MyImga=findViewById(R.id.StudentIMG);        StudenName=findViewById(R.id.edtName);        StuId=findViewById(R.id.editID);    }    @Override    protected void onActivityResult(int requestCode, int resultCode, Intent data) {        if(requestCode == REQUEST_CODE_GALLERY && resultCode == RESULT_OK && data != null){            Uri uri = data.getData();            try {                InputStream inputStream = getContentResolver().openInputStream(uri);                Bitmap bitmap = BitmapFactory.decodeStream(inputStream);                MyImga.setImageBitmap(bitmap);            } catch (FileNotFoundException e) {                e.printStackTrace();            }        }    }    @Override    public void onRequestPermissionsResult(int requestCode, @NonNull String[] permissions, @NonNull int[] grantResults) {        if(requestCode == REQUEST_CODE_GALLERY){            if(grantResults.length >0 && grantResults[0] == PackageManager.PERMISSION_GRANTED){                Intent intent = new Intent(Intent.ACTION_PICK);                intent.setType("image/*");                startActivityForResult(intent, REQUEST_CODE_GALLERY);            }            else {                Toast.makeText(getApplicationContext(), "You don't have permission to access file location!", Toast.LENGTH_SHORT).show();            }            return;        }        super.onRequestPermissionsResult(requestCode, permissions, grantResults);    }    public static byte[] imageViewToByte(ImageView image) {        Bitmap bitmap = ((BitmapDrawable)image.getDrawable()).getBitmap();        ByteArrayOutputStream stream = new ByteArrayOutputStream();        bitmap.compress(Bitmap.CompressFormat.JPEG, 100, stream);        byte[] byteArray = stream.toByteArray();        return byteArray;    }}