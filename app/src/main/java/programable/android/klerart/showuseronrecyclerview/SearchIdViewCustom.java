package programable.android.klerart.showuseronrecyclerview;import androidx.appcompat.app.AppCompatActivity;import androidx.recyclerview.widget.LinearLayoutManager;import androidx.recyclerview.widget.RecyclerView;import android.content.Intent;import android.os.Bundle;import android.view.View;import android.widget.SearchView;import android.widget.Toast;import programable.android.klerart.showuseronrecyclerview.Database.Database;import programable.android.klerart.showuseronrecyclerview.MyAdapter.SearchAdapter;public class SearchIdViewCustom extends AppCompatActivity {    Database database;    SearchAdapter adapter;    RecyclerView recyclerView;    RecyclerView.LayoutManager layoutManager;    SearchView SearchBtn;    @Override    protected void onCreate(Bundle savedInstanceState) {        super.onCreate(savedInstanceState);        setContentView(R.layout.activity_search_id_view_custom);        database=new Database(this);        SearchBtn=findViewById(R.id.SearchBtn);        recyclerView=findViewById(R.id.MyRecyleView);        adapter=new SearchAdapter(this,database.getAllStudent());        layoutManager=new LinearLayoutManager(this);        recyclerView.setLayoutManager(layoutManager);        recyclerView.setHasFixedSize(true);        recyclerView.setAdapter(adapter);        SearchBtn.setOnQueryTextListener(new SearchView.OnQueryTextListener() {            @Override            public boolean onQueryTextSubmit(String SoecName) {                // adapter=new SearchAdapter(SearchViewCustom.this,database.getDependT0Number(SoecName));                // recyclerView.setAdapter(adapter);                return false;            }            @Override            public boolean onQueryTextChange(String SoecName) {                try {                    adapter=new SearchAdapter(SearchIdViewCustom.this,database.getDependToID(SoecName));                    recyclerView.setAdapter(adapter);                    return false;                }catch (Exception e){                    Toast.makeText(SearchIdViewCustom.this, ""+e.getMessage(), Toast.LENGTH_SHORT).show();                    return false;                }            }        });    }    public void SwitchActive(View view){        startActivity(new Intent(this,SearchViewCustom.class));    }}