package programable.android.klerart.showuseronrecyclerview.Database;import android.content.Context;import android.database.Cursor;import android.database.sqlite.SQLiteDatabase;import android.database.sqlite.SQLiteQueryBuilder;import com.readystatesoftware.sqliteasset.SQLiteAssetHelper;import java.util.ArrayList;import java.util.List;import programable.android.klerart.showuseronrecyclerview.Modell.Student;public class Database extends SQLiteAssetHelper {    private static final String DB_NAME="Students - Copy.sqlite";    private static final int DB_Version=1;    public Database(Context context) {        super(context, DB_NAME, null, DB_Version);    }    public List<Student> getAllStudent(){        SQLiteDatabase db=getReadableDatabase();        SQLiteQueryBuilder qb=new SQLiteQueryBuilder();        String[] SqlSelect={"ID","FullName","Semester","Count"};        String tableName="Student";        qb.setTables(tableName);        Cursor cursor=qb.query(db,SqlSelect,null,null,null,null,null);        System.out.println("count : " +cursor.getCount());        List<Student> result=new ArrayList<>();        if(cursor.moveToFirst()){            do {                Student student=new Student();                student.setStudentId(cursor.getInt(cursor.getColumnIndex("ID")));                student.setStuName(cursor.getString(cursor.getColumnIndex("FullName")));                student.setStuSemester(cursor.getInt(cursor.getColumnIndex("Semester")));                student.setStuGroup(cursor.getInt(cursor.getColumnIndex("Count")));//                student.setStuGroup(cursor.getInt(cursor.getColumnIndex("Group")));                result.add(student);            }while (cursor.moveToNext());        }        return result;    }    public List<Student> getAStudentBayg(){        SQLiteDatabase db=getReadableDatabase();        SQLiteQueryBuilder qb=new SQLiteQueryBuilder();        String[] SqlSelect={"ID","FullName","Semester","Count"};        String tableName="Student";        qb.setTables(tableName);        Cursor cursor=qb.query(db,SqlSelect," Semester LIKE 4",null,null,null,null);        System.out.println("count : " +cursor.getCount());        List<Student> result=new ArrayList<>();        if(cursor.moveToFirst()){            do {                Student student=new Student();                student.setStudentId(cursor.getInt(cursor.getColumnIndex("ID")));                student.setStuName(cursor.getString(cursor.getColumnIndex("FullName")));                student.setStuSemester(cursor.getInt(cursor.getColumnIndex("Semester")));                student.setStuGroup(cursor.getInt(cursor.getColumnIndex("Count")));//                student.setStuGroup(cursor.getInt(cursor.getColumnIndex("Group")));                result.add(student);            }while (cursor.moveToNext());        }        return result;    }    //function to get all student Name;    public List<String> getNames(){        //============================================        SQLiteDatabase db=getReadableDatabase();        SQLiteQueryBuilder qb=new SQLiteQueryBuilder();        String[] SqlSelect={"FullName"};        String tableName="Student";        qb.setTables(tableName);        Cursor cursor=qb.query(db,SqlSelect,null,null,null,null,null);        List<String> result=new ArrayList<>();        if (cursor.moveToNext()){            do {                result.add(cursor.getString(cursor.getColumnIndex("FullName")));            }while (cursor.moveToNext());        }        return result;        //==============================================    }    public void Database(){    }    public List<Student> getStudentByName(){        SQLiteDatabase db=getReadableDatabase();        SQLiteQueryBuilder qb=new SQLiteQueryBuilder();        String[] SqlSelect={"ID","FullName","IMGStudent","Semester","Department","Group","Count"};        String tableName="Student";        qb.setTables(tableName);        //this Qyery Where Name Like if u want extract name just change        //Cursor cursor=qb.query(db,SqlSelect,"FullName LIKE ?",new String[]{name},null,null,null);        //this will query name        Cursor cursor=qb.query(db,SqlSelect,null,null,null,null,null);        List<Student> result=new ArrayList<>();        if (cursor.moveToNext()){            do {                Student student=new Student();                student.setStudentId(cursor.getInt(cursor.getColumnIndex("ID")));                student.setStuName(cursor.getString(cursor.getColumnIndex("FullName")));                student.setStuSemester(cursor.getInt(cursor.getColumnIndex("Semester")));                student.setStuDepartment(cursor.getInt(cursor.getColumnIndex("Department")));                student.setStuGroup(cursor.getInt(cursor.getColumnIndex("Group")));                result.add(student);            }while (cursor.moveToNext());        }        //==================================================================================        //======================================================================================        return result;    }}